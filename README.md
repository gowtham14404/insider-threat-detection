# insider-threat-detection
🛡️ Real-time insider threat detection using behavioral biometrics (keystroke dynamics, mouse analytics &amp; process monitoring) with One-Class SVM + LSTM Autoencoder dual AI models. Privacy-safe — raw keystrokes never stored. Built with FastAPI, Scikit-learn, NumPy &amp; Docker.
